# Note: This workflow is deprecated
# Cloud and app deployments are now handled by their respective repositories:
# - app repository: handles app builds and releases
# - cloud repository: handles cloud service deployments
# This file is kept for reference but will not run automatically
name: "Main → Deploy to Staging (Deprecated)"

on:
  workflow_dispatch:
    inputs:
      skip:
        description: "This workflow is deprecated - use cloud repository workflows instead"
        required: false

jobs:
  deprecated:
    name: Deprecated Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️  This workflow is deprecated"
          echo ""
          echo "Cloud and app deployments are now handled by their respective repositories:"
          echo "  - app repository: https://github.com/brewos-io/app"
          echo "  - cloud repository: https://github.com/brewos-io/cloud"
          echo ""
          echo "Please use the workflows in those repositories for deployments."
