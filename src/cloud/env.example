# Server
PORT=3001
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:5173

# Data directory (for SQLite database)
DATA_DIR=./data

# Supabase Auth - Option 1: Legacy JWT Secret
# Get from: Supabase Dashboard > Settings > API > JWT Settings
# SUPABASE_JWT_SECRET=your-jwt-secret

# Supabase Auth - Option 2: JWKS (Recommended)
# Just provide your project URL, keys are fetched automatically
SUPABASE_URL=https://your-project.supabase.co

# Web UI (built React app path)
WEB_DIST_PATH=../web/dist
